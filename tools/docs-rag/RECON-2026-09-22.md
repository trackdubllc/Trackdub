# Docs reconciliation report — 2026-09-22

Reconciliation of Trackdub first-party GPU/provider docs against pinned vendor docs, per [reconcile-prompt.md](reconcile-prompt.md). Run started on branch `agent/docs-rag-sync-and-mirrors` (spec read at commit `aee3492`); the repo was switched to `trtrtx-handoff` (`cdf6390`) by concurrent activity mid-run. All quoted first-party files are byte-identical between the two branches (verified via `git diff agent/docs-rag-sync-and-mirrors..HEAD -- docs/ AGENTS.md runtime/ src/Trackdub.Inference/Runtime/Planning/StageRuntimeRequirements.cs`: empty), so every quote and line number below holds on both.

**Follow-up pass (2026-09-22, later):** a first revision of this report marked Pairs 4/5 blocked on a missing `trackdub-docs-rag` MCP. That conclusion was wrong on reachability: the hosted `trackdub-docs-rag` Worker is reachable over plain HTTPS from the shell even though the MCP client isn't connected in-session, and **vendor R2 keys end in `.md`** (`tools/docs-rag/sync_corpus.py` line ~126 stages vendors as `<prefix>/<source_id>.md`, e.g. `vendor/olive/olive-landing.md`, `vendor/amd/migraphx-install.md`); the earlier not-found probes failed only on the missing suffix. All vendor sources the seed mapping requires now fetched successfully via the hosted `get_trackdub_doc` (stateless MCP-over-HTTP, SSE response) with `max_chars` 200000 — every fetch `found: true`, none truncated. The substitute-corpus caveat below is therefore **superseded for every rechecked pair** (marked "recheck" inline); it remains accurate only for the first-pass verifications that were not re-run against the mapped sources.

## Provenance and corpus (current state)

- **Vendor canon (all pairs):** pinned vendor docs as stored in the hosted R2 corpus, fetched via `get_trackdub_doc` over direct HTTPS (`https://trackdub-docs-rag.trackdub.workers.dev/mcp`, bearer auth from `.mcp.json`). Vendor keys: `<prefix>/<source-id>.md`. No vendor URL was fetched from the network.
- **Retrieval checks:** hosted `/v1/search` (scope `all`, limit 8), plus the first pass's `trackdub-gpu-docs` keyword searches where noted.
- First pass only (superseded where marked): the `trackdub-docs-rag` MCP was not connected in-session, so an overlapping corpus MCP (`trackdub-gpu-docs`, 23 sources, same pin block, `matchesCorpusPin: true`) was used for the initial vendor reads. Its vendor set overlapped NVIDIA EP ABI GitHub + ORT TRT-RTX/plugin-EP + Microsoft WinML/DirectML pages; every mapping-required source not in it (AMD, Olive, per-EP ORT pages, NVIDIA /latest/ extras) is now covered by hosted R2 fetches in this revision.

**Retrieval-hygiene note — synthesized `docs-rag-pin.md`:** the hosted index contains `first-party/trackdub/docs/reference/docs-rag-pin.md`, which **does not exist in the repo** — `tools/docs-rag/sync_corpus.py:147-160` (`stage_pin`) synthesizes it into the staging tree at sync time, asserting the pin (EP ABI 0.3.0 cu12, `tensorrt_rtx_1_5.dll`, do-not-treat-catalog-EP-as-primary, "first-party docs win on disagreement"). It is corpus-by-design, not drift. It ranked #1 on two of the five hosted-index retrieval checks (EP-name spelling, ORT-floor queries), where its content is correct and helpful. Hygiene concern, not a defect: it is a first-party-labeled, retrieval-dominant document that repo tooling (linters, link checkers, reviewers) cannot see; if the pin manifest ever drifts from `stage_pin`'s hardcoded text, the corpus would keep asserting the old pin with first-party authority. Worth a follow-up comment in `stage_pin` referencing the manifest as source of truth.

Retrieval-verification notes: the hosted AI Search index currently has ~25 outdated items (capacity errors) per `HANDOFF.md`, so any hosted-index result can serve stale chunks; suspected cases are flagged inline. Per the prompt's ground rules: **canon for upstream facts = pinned vendor docs as served by the corpus; canon for Trackdub decisions = `runtime/trt-rtx-ep.manifest.json` + `docs/decisions/*` + `AGENTS.md`** (and per AGENTS.md conflict order, source code outranks docs for Trackdub behavior — used for the Pair 5 verdicts below). First-party quotes were read from the local repo on disk; line numbers are from this branch's working tree.

---

## Pair 1 — `docs/reference/tensorrt-rtx-ep-abi-plugin.md` <-> NVIDIA EP ABI release/README + ORT TRT-RTX EP + ORT plugin-EP usage

First-party key: `first-party/trackdub/docs/reference/tensorrt-rtx-ep-abi-plugin.md`

### 1.1 Build targets: TRT RTX on both TFMs

| | |
|---|---|
| First-party | `docs/reference/tensorrt-rtx-ep-abi-plugin.md:10-15` — "TRT RTX works on **both** Windows target frameworks because the EP ABI plugin is plain ONNX Runtime extensibility: it does not need the Windows ML packages" |
| Vendor | `gh-ep-abi-readme`: "This EP is built as a standalone plugin (`onnxruntime_providers_nv_tensorrt_rtx.dll`) that implements the ORT EP ABI interfaces (`OrtEpFactory`, `OrtEp`, `OrtNodeComputeInfo`, `OrtDataTransferImpl`, etc.) introduced in ORT 1.23.0. It does not need to be built together with ONNX Runtime." |
| Classification | **agree** (no delta) |
| Retrieval | Query "TensorRT RTX Windows ML catalog EP NvTensorRtRtx standalone plugin route": top hit is this doc's chunk restating exactly this; retrieval leads to the right answer. No contradiction in results. |

### 1.2 EP device identity / spelling

| | |
|---|---|
| First-party | `docs/reference/tensorrt-rtx-ep-abi-plugin.md:24-28` — "selects the GPU `OrtEpDevice` whose EP name is: `NvTensorRTRTXExecutionProvider` [...] Do not accept `NvTensorRtRtxExecutionProvider` as the primary plugin identity. That spelling belonged to the deprecated Windows ML catalog wiring" |
| Vendor | `gh-ep-abi-readme` usage code: `env.RegisterExecutionProviderLibrary("NvTensorRTRTXExecutionProvider", ...)` and `std::strcmp(ep_device.EpName(), "NvTensorRTRTXExecutionProvider") == 0`. Windows ML catalog pages (`ms-winml-supported-eps`) use EpName `"NvTensorRtRtxExecutionProvider"` for the WinML catalog EP; ORT built-in (deprecated) Python example uses `'NvTensorRtRtxExecutionProvider'` string form. Two distinct devices exist with two distinct spellings. |
| Classification | **trackdub-deliberate** — the two spellings are genuinely two different devices (WinML catalog EP vs standalone ABI plugin), and Trackdub's pin (manifest + ADR-0002 point 2) is the standalone plugin. The doc's "deprecated Windows ML catalog wiring" phrasing is slightly stale though: the WinML catalog EP is not "deprecated", it is a *different, still-supported* Windows ML route (`ms-winml-supported-eps` lists it as currently available). |
| Retrieval | Query "NvTensorRtRtxExecutionProvider deprecated Windows ML catalog NvTensorRTRTXExecutionProvider": top hits are ADR-0002 and this doc (correct reconciled answer), with `ms-winml-supported-eps` chunk 3 also surfacing the `NvTensorRtRtxExecutionProvider` EpName so an agent sees both spellings and their distinct provenance. Leads to reconciled answer; no contradiction, but note ORT's own deprecated-EP Python snippet (`providers=['NvTensorRtRtxExecutionProvider']`) still floats the Rt-Rtx spelling in results; flagged as **potential stale/legacy vendor chunk** — `ort-tensorrt-rtx-ep` chunk 0 already marks the built-in EP deprecated, so an agent reading top hits resolves correctly. |
| Proposed edit | ```diff
- Do not accept `NvTensorRtRtxExecutionProvider` as the primary plugin identity. That spelling belonged to the deprecated Windows ML catalog wiring and must not be used for the standalone plugin route.
+ Do not accept `NvTensorRtRtxExecutionProvider` as the primary plugin identity. That spelling is the **Windows ML catalog EP** (`NvTensorRtRtx`, still offered by Windows ML as a separate route per Microsoft's supported-EPs page) and the deprecated ORT built-in EP; it names a different device from the standalone plugin route Trackdub pins. Vendor default offers both; Trackdub pins the standalone `NvTensorRTRTXExecutionProvider` plugin because ADR-0002 routes TRT RTX through the EP ABI plugin, not the Windows ML catalog.
``` |

### 1.3 Windows required bundle triple

| | |
|---|---|
| First-party | `docs/reference/tensorrt-rtx-ep-abi-plugin.md:36-40` — required files `onnxruntime_providers_nv_tensorrt_rtx.dll`, `tensorrt_rtx_1_5.dll`, `tensorrt_onnxparser_rtx_1_5.dll`; `tensorrt_plugins.dll` copied but "not part of the required readiness triple" (line 48-49) |
| Vendor | Pin block (corpus): `bundledWindowsLibs` = exactly those three. `gh-ep-abi-readme` output-lib statement names only `onnxruntime_providers_nv_tensorrt_rtx.dll`; the `_1_5` naming comes from TRT-RTX 1.5 lineage per corpus note ("DLL suffix _1_5 means TensorRT-RTX library lineage 1.5 inside EP ABI 0.3.0"). Pin-accurate canon = manifest + release assets. |
| Classification | **agree** (matches pin manifest exactly) |
| Retrieval | `list_corpus` pin block is surfaced on every search call; retrieval cannot miss it. No contradiction. |

### 1.4 Linux bundle layout

| | |
|---|---|
| First-party | `docs/reference/tensorrt-rtx-ep-abi-plugin.md:42-49` — Linux (v0.3.0 cu12 linux-x64 tarball): `libonnxruntime_providers_nv_tensorrt_rtx.so`, `libtensorrt_rtx.so`, `libtensorrt_onnxparser_rtx.so`, `libtensorrt_plugins.so` |
| Vendor | `gh-ep-abi-readme` build-output section: "Windows: `build\Release\onnxruntime_providers_nv_tensorrt_rtx.dll` / Linux: `build/libonnxruntime_providers_nv_tensorrt_rtx.so`"; v0.3.0 release notes confirm "Linux SONAME/symlink handling" and per-CUDA variants (cu12, cu13). TRT-RTX runtime libs on /latest/ docs are `libtensorrt_rtx.so` / `libtensorrt_onnxparser_rtx.so` (`nv-trt-rtx-porting`, `nv-trt-rtx-how-it-works` "Runtime Library" section). |
| Classification | **agree** for the v0.3.0 cu12 pin. (Upstream /latest/ docs now describe `tensorrt_rtx_1_6.dll` naming for TRT-RTX 1.6; that is a *newer* product doc tree than the pin and per corpus note must not be treated as pin canon. Not a defect.) |
| Retrieval | Query "EP ABI 0.3.0 ORT 1.25 1.24 compatibility matrix TensorRT RTX 1.5": top hits include `gh-ep-abi-readme` chunk 1 (compatibility matrix: EP 0.3 -> ORT 1.25.0+, TRT RTX 1.5.x.x, "Linux Support, Weight Streaming, CIG Interop, ORT Version Negotiation") and chunk 4 (cu12 wheel for 0.3.0, `trt_rtx_home ...TensorRT-RTX-1.5.0.114`). Leads to the reconciled answer. No contradiction. |

### 1.5 ORT version compatibility

| | |
|---|---|
| First-party | Not stated explicitly in the doc; implied by pin (0.3.0 cu12). No claim extracted. |
| Vendor | `gh-ep-abi-readme` compatibility matrix: EP 0.1 -> ORT 1.24.0+ / TRT-RTX 1.4.x.x (Windows only); **EP 0.3 -> ORT 1.25.0+ / TRT-RTX 1.5.x.x** (adds Linux, weight streaming, CIG interop, ORT version negotiation). v0.3.0 release notes: "Negotiate ORT API version with the host so one DLL serves ONNX Runtime 1.24-1.26+". Build prerequisites list "ONNX Runtime SDK 1.24.0+" (build-time; runtime floor for the wheel is the 1.25.0+ row). |
| Classification | **unverifiable -> needs-human (gap, not mismatch)**: the first-party doc never states the ORT managed-package floor for the pinned EP. Windows-ml-phase-4-closeout mentions "Managed ORT 1.24.x vs WinML-bundled native 1.17.x" history, but no current statement ties EP ABI 0.3.0 to an ORT C# package requirement (vendor matrix says ORT 1.25.0+ / negotiates 1.24-1.26+). Propose adding one sentence so the doc carries the vendor floor itself. |
| Retrieval | Covered by the 1.4 query; `gh-ep-abi-readme` chunk 1 is the top-versioned hit. Hosted-index check (query 4 below) shows the R2 index does NOT surface this vendor matrix for the ORT-floor question, strengthening the case for the doc edit. |
| Recheck (follow-up, mapped R2 source) | `vendor/nvidia/ep-abi-readme.md` confirms verbatim: "0.3 | 1.25.0+ | 1.5.x.x | Linux Support, Weight Streaming, CIG Interop, ORT Version Negotiation", plus "CIG interop test cases require ORT SDK 1.25+ (ORT_API_VERSION >= 25). With ORT 1.24, only the EP registration smoke test compiles." **Classification unchanged: gap, proposed sentence stands.** |
| Proposed edit | ```diff
  ## Bundle channel (in-app + manifest)
  Trackdub ships the EP ABI plugin through a pinned manifest, not the Windows ML catalog:
  - **Manifest:** `runtime/trt-rtx-ep.manifest.json` (version `0.3.0`, CUDA `cu12`, per-RID archive URL + SHA-256 + size).
+ - **Host ORT floor:** NVIDIA's EP ABI 0.3.0 compatibility matrix lists ONNX Runtime 1.25.0+ (the plugin negotiates the ORT API version so one DLL serves ORT 1.24-1.26+); TensorRT-RTX runtime lineage is 1.5.x (hence the `_1_5` DLL suffix).
  - **Composition copy:** ...
``` |

### 1.6 Session bootstrap never downloads the EP bundle

| | |
|---|---|
| First-party | `docs/reference/tensorrt-rtx-ep-abi-plugin.md:79` (inference bootstrap "registers an already-installed bundle only; it **never** downloads the EP bundle") and the download-policy table lines 85-91 |
| Vendor | `ms-winml-initialize-eps`: "Windows ML EPs are downloaded by Windows when your app calls `EnsureReadyAsync()` or `EnsureAndRegisterCertifiedAsync()`." Vendor default is download-on-demand inside catalog APIs; Trackdub deliberately keeps all downloads on install paths (Model Manager / CLI / license-accepted benchmark opt-in). |
| Classification | **trackdub-deliberate** — deviation from the vendor's download-anywhere pattern, justified by ADR-0002 point 5 ("Runtime routing stays manifest-driven, stage-scoped, and smoke-test verified"; license consent policy in ADR-0002 Phase 5 licensing) and AGENTS.md "Never fake readiness". |
| Retrieval | Query "session bootstrap never downloads provider bundle install license gate": top hits are this doc's chunks 2-3 (exact policy table) and ADR-0002 chunk 8 (license families). Leads to reconciled answer. Vendor chunks in results (`ms-winml-supported-eps` WebGPU chunk) don't contradict; they describe catalog-EP download, a different route. No contradiction. |
| Proposed edit | ```diff
  - **Inference bootstrap:** registers an already-installed bundle only; it **never** downloads the EP bundle (same policy as WinML catalog EPs during session bootstrap).
+   Vendor default (Windows ML catalog) downloads EPs on demand inside `EnsureReadyAsync()` / `EnsureAndRegisterCertifiedAsync()`; Trackdub keeps all downloads on explicit install paths because ADR-0002 (points 2 and 5, Phase 5 licensing) gates every EP/plugin install behind `ILicenseConsentService` acceptance, and session bootstrap must not mutate provider state mid-run.
``` |

### 1.7 ORT GenAI `NvTensorRtRtx` exclusion

| | |
|---|---|
| First-party | `docs/reference/tensorrt-rtx-ep-abi-plugin.md:171-173` — "ORT GenAI's `NvTensorRtRtx` device can terminate the host process with a native stack overflow during model init/generation (observed on `qwen-instruct`, Qwen2.5-1.5B) [...] the planner never offers TensorRT to GenAI-loaded families and the smoke tester refuses the combination" |
| Vendor | Pinned vendor docs are silent on any GenAI crash. `nv-trt-rtx-arch-overview` actively advertises the opposite: "ONNX Runtime GenAI — LLM workflows on RTX with TensorRT-RTX as an execution provider" (and "TensorRT-RTX is the default EP when Windows ML runs on supported RTX hardware"). `ms-winml-supported-eps` separately notes MIGraphX "is not supported for GenAI scenarios today" (AMD only). |
| Classification | **trackdub-deliberate** — a Trackdub-observed defect guard against a vendor-advertised path. Canon here is code + ADR-0002 Phase 6 ("ORT GenAI model loads never see TensorRT. GenAI's `NvTensorRtRtx` device terminated the host process (native stack overflow) on a bundled `qwen-instruct` model"). The reconcile-prompt's classification set has no "first-party knows better" bucket; the deviation is deliberate, recorded in ADR-0002 Phase 6, and verified against `StageRuntimeRequirements.cs` (engine-family overrides for `whisper-genai`, `phi-genai`, `qwen-instruct` all route through `WithoutTensorRtFamilies`). Keep, and add the vendor-position sentence so either source alone carries the truth. |
| Retrieval | Query "ORT GenAI NvTensorRtRtx stack overflow terminate process qwen-instruct TensorRT exclusion": top hits are this doc chunk 5, ADR-0002 chunk 7 (Phase 6), and stage-matrix chunk 5 (smoke results "fail (refused)"). All lead to the Trackdub answer. **Contradiction present in results**: `nv-trt-rtx-arch-overview` (retrieved in the 1.4/D8 query set) advertises GenAI-on-TRT-RTX as a supported complementary path. An agent searching only vendor chunks would conclude GenAI+TRT-RTX is supported. Flagged: this is a genuine first-party-vs-vendor content contradiction that retrieval surfaces, not a stale-chunk artifact. |
| Recheck (follow-up, mapped R2 source) | `vendor/nvidia/trt-rtx-arch.md` confirms the same statements under the mapped key: complementary-software table lists "ONNX Runtime GenAI — LLM workflows on RTX with TensorRT-RTX as an execution provider", and "When used as an execution provider with Windows ML or ONNX Runtime GenAI, TensorRT-RTX optimizes LLM inference on RTX GPUs with compute capability 8.6 or later (Ampere and later)." Vendor position unchanged under the mapped key; Trackdub's exclusion stands on ADR-0002 Phase 6 + observed crash. **Classification unchanged: trackdub-deliberate.** |
| Proposed edit | ```diff
  **ORT GenAI loads are excluded from TensorRT entirely** (`whisper-genai`, `phi-genai`, `qwen-instruct` engine-family overrides). ORT GenAI's `NvTensorRtRtx` device can terminate the host process with a native stack overflow during model init/generation (observed on `qwen-instruct`, Qwen2.5-1.5B). A fatal crash cannot surface as a catchable smoke failure, so the planner never offers TensorRT to GenAI-loaded families and the smoke tester refuses the combination before touching native code.
+ NVIDIA's current TensorRT-RTX architecture docs list ONNX Runtime GenAI as a supported integration (LLM workflows with TRT-RTX as EP); that is the vendor's position against a newer product line than the pinned EP ABI 0.3.0 / TRT-RTX 1.5 lineage, and it does not override the crash Trackdub observed on the pinned stack (ADR-0002 Phase 6). Revisit only when the EP pin is bumped and the crash is retested.
``` |

### 1.8 CUDA-graph default, provider options

| | |
|---|---|
| First-party | No claims; the doc does not document `enable_cuda_graph` or other EP provider options. |
| Vendor | `vendor/onnxruntime/trt-rtx-ep.md` (recheck, mapped source): "CUDA Graph is enabled by default. To completely disable CUDA Graph, you can set the `enable_cuda_graph` provider option to `False` (or `0`)." Options table: `device_id` (default 0), `enable_cuda_graph` (default true), `nv_runtime_cache_path` (default "" disabled), `nv_multi_profile_enable` (default false), `nv_max_workspace_size` / `nv_max_shared_mem_size` (0 auto), `nv_dump_subgraphs` / `nv_detailed_build_log` (false), profile-shape options (auto), `nv_use_external_data_initializer` (false). "All the options listed below apply to both the built-in TensorRT RTX EP and the standalone EP ABI plugin." v0.3.0 release notes: "auto-disable CUDA Graphs when weight streaming is enabled". |
| Classification | **unverifiable (gap)** — vendor documents EP options Trackdub's doc never mentions; no first-party claim to contradict. The mapped-source fetch confirms the substitute read verbatim (same table, same defaults, same deprecation notice for the built-in EP, same `NvTensorRtRtxExecutionProvider` string in the deprecated Python snippet). Unchanged. Out of reconciliation scope to import wholesale; note for a possible follow-up doc section. No edit proposed in this report (report-only constraint). |

### 1.9 `ORT_LOG_SEVERITY_LEVEL` probe-noise guidance

| | |
|---|---|
| First-party | `docs/reference/tensorrt-rtx-ep-abi-plugin.md:182` — `$env:ORT_LOG_SEVERITY_LEVEL = "3" # 0=Verbose .. 4=Fatal` and "Trackdub does not rewrite ORT's default log severity in session bootstrap" |
| Vendor | `ort-plugin-ep-usage` / `gh-ep-abi-readme` sample code creates envs with `ORT_LOGGING_LEVEL_WARNING`; no vendor doc in the fetched set documents `ORT_LOG_SEVERITY_LEVEL` as an env var or its 0-4 scale. (ORT's global runtime doc `env-var` page is not in either corpus.) |
| Classification | **unverifiable** — vendor docs fetched are silent on the env var; the 0-4 severity scale matches ORT's `LoggingLevel` enum ordering but cannot be confirmed from the pinned corpus set. Doc statement is plausible and consistent with ORT's enum; leave as-is, mark for verification when the ORT env-var doc is added to the corpus. Recheck (follow-up): scanned `vendor/nvidia/trt-rtx-c-api.md`, `trt-rtx-python-api.md`, `trt-rtx-inference-library.md`, `trt-rtx-advanced.md` — zero mentions of `ORT_LOG_SEVERITY_LEVEL` or any ORT-side env-var log control (the c-api doc covers `ILogger`/`Severity` for the native TRT-RTX API only). **Unchanged: unverifiable** — the ORT env-var documentation page remains outside the corpus. |

### 1.10 Engine cache clearing guidance

| | |
|---|---|
| First-party | `docs/reference/tensorrt-rtx-ep-abi-plugin.md:127-129` — "Clear the engine cache after a GPU driver change, GPU swap, or TRT RTX EP version bump when inference fails with stale compiled engines" |
| Vendor | `nv-trt-rtx-index` (1.6 release notes, upstream-latest not pin canon): "Runtime Cache Compatibility Tightened — The runtime environment must match the cached GPU SKU, TensorRT-RTX version, and Compute-in-Graphics (CiG) state, and the driver version must be greater than or equal to the cached version." `nv-trt-rtx-arch-overview`: "Serialized engines are tied to the TensorRT-RTX version that built them." |
| Classification | **agree** (guidance direction matches upstream compatibility rules; upstream-latest wording is stricter than the pin-era behavior but not contradictory — Trackdub's "when inference fails" trigger is the conservative pin-era form). No edit. Note for a future pin bump: the 1.6 "must match" language will make clearing *mandatory*, not advisory, once Trackdub moves past the 1.5 lineage. |
| Recheck (follow-up, mapped R2 sources) | `vendor/nvidia/trt-rtx-troubleshooting.md` "Engine fails to load or JIT compile on the target machine: **Verify GPU support, driver version, and that the engine was built for the installed TensorRT-RTX release**" — matches the doc's clear-after-driver-change/version-bump guidance in substance. `vendor/nvidia/trt-rtx-best-practices.md` points at "Working with Runtime Cache" and "runtime caching, and warmup for JIT-compiled kernels" but its own body has no clearing policy. `vendor/nvidia/trt-rtx-support-matrix.md` covers engine portability by compute capability (8.6/8.9/12.0/12.1 default; Turing 7.5 excluded by default), not cache invalidation. **Classification unchanged: agree.** The `trt-rtx-how` runtime-library/compatibility statements previously quoted via substitute remain the strongest pin-era vendor support for the guidance. |

### 1.11 Smoke/status/install/download-policy claims

| | |
|---|---|
| First-party | `docs/reference/tensorrt-rtx-ep-abi-plugin.md:194` (`trackdub providers trt-rtx smoke`), `:200` (test filter), `:229` (`install --accept-license`), `:228/230` (status/doctor) — all Trackdub-specific CLI surfaces |
| Vendor | No vendor counterpart; these are Trackdub product claims. Not checkable against vendor docs. **agree (out of vendor scope)**. Readiness-states list (lines 144-152) matches AGENTS.md "Provider registered != model downloaded != stage ran != stage succeeded" verbatim in spirit; AGENTS.md is canon and the doc restates it faithfully. |

### 1.12 Fallback chain (Windows): TRT RTX -> DirectML -> CPU

| | |
|---|---|
| First-party | `docs/reference/tensorrt-rtx-ep-abi-plugin.md:18-19` |
| Vendor | `nv-trt-rtx-arch-overview` complementary-software table: "Windows ML — Default execution provider on RTX GPUs for production Windows apps. TensorRT-RTX is the default EP when Windows ML runs on supported RTX hardware." I.e. vendor's Windows story routes TRT-RTX *through* Windows ML; Trackdub's chain places the standalone plugin first and keeps WinML/DirectML as fallback. |
| Classification | **trackdub-deliberate** per ADR-0002 (TRT RTX handled as standalone plugin, DirectML as legacy fallback). The upstream-latest docs' "Windows ML default EP on RTX" framing describes the Windows-ML-managed route Trackdub deliberately does not use for TRT RTX. No edit needed to the chain itself; the existing doc already says "not a Windows ML catalog EP". |

---

## Pair 2 — `docs/reference/gpu-execution-providers.md` <-> ORT TRT-RTX EP / plugin-EP / WinML docs

First-party key: `first-party/trackdub/docs/reference/gpu-execution-providers.md`

### 2.1 Per-TFM provider reach table

| | |
|---|---|
| First-party | `docs/reference/gpu-execution-providers.md:10-15` — portable `net10.0`: CPU + TensorRT RTX; `net10.0-windows10.0.19041.0`: adds DirectML + WinML catalog EPs; Linux net10.0 adds native CUDA/TensorRT via system ORT GPU build |
| Vendor | `gh-ep-abi-readme` compatibility matrix: EP 0.3 supports Linux (EP 0.1 was Windows-only); `ms-winml-overview`: hardware-optimized EPs require Windows 11 24H2 (build 26100)+. No vendor doc claims DirectML needs a Windows TFM; that is a .NET packaging fact (WinRT/WindowsAppSDK packages), Trackdub-specific. |
| Classification | **agree** for TRT-RTX reach (Linux support in 0.3.0 confirmed by vendor matrix); the DirectML/WinML-Windows-TFM claim is Trackdub packaging reality, not vendor-contradicted. |
| Recheck (follow-up, mapped R2 sources) | `vendor/onnxruntime/directml-ep.md`: "DirectML is a high-performance, hardware-accelerated DirectX 12 library for machine learning **on Windows**" — vendor confirms DirectML is Windows-only, consistent with the first-party Windows-TFM row. `vendor/onnxruntime/cuda-ep.md` carries Windows/Linux CUDA + cuDNN version tables (e.g. 8.5.0.96 Windows / 8.2.4 Linux) — consistent with the Linux native CUDA row. `vendor/onnxruntime/ep-overview.md` lists CUDA / TensorRT / TensorRT RTX / OpenVINO / DirectML / QNN / MIGraphX as sibling EPs with no cross-EP ordering claims. `vendor/onnxruntime/qnn-ep.md`: "used on Android and Windows devices with Qualcomm Snapdragon SOC's" (consistent with Trackdub's future-NPU framing). `vendor/onnxruntime/migraphx-ep.md` is ROCm/Linux-oriented; `vendor/onnxruntime/openvino-ep.md` covers Ubuntu + Windows wheels. Nothing contradicts the first-party per-TFM table. **Classification unchanged: agree.** |

### 2.2 CLI: `cuda`/`tensorrt` accepted as compatibility aliases for `trt-rtx` (Windows)

| | |
|---|---|
| First-party | `docs/reference/gpu-execution-providers.md:44` |
| Vendor | `ort-tensorrt-rtx-ep` deprecated built-in path used `kNvTensorRTRTXExecutionProvider`; ORT's separate CUDA-EP and TensorRT-EP pages (`cuda-ep`, `trt-ep` — not in the reachable corpus) describe distinct providers. Windows ML lists `NvTensorRtRtx` as its own catalog EP. |
| Recheck (follow-up, mapped R2 sources) | `vendor/onnxruntime/cuda-ep.md` and `vendor/onnxruntime/ep-overview.md` confirm CUDA EP and TensorRT RTX EP are distinct vendor providers with distinct names — which sharpens the open question (aliasing two distinct vendor providers onto one Trackdub kind is a UX decision, not a vendor-endorsed equivalence), but no vendor doc endorses or forbids the aliasing. QNN/OpenVINO/MIGraphX pages don't touch it. |
| Classification | **needs-human** — unchanged. The aliasing is a Trackdub CLI UX choice (source canon: CLI parser); whether "tensorrt"/"cuda" should silently alias to `trt-rtx` on Windows rather than the distinct native `TensorRt`/`Cuda` providers that also appear in the milestone order (`docs/reference/gpu-execution-providers.md:33-34`) is a product decision the vendor docs cannot arbitrate. Mark for a CLI-source follow-up. |

### 2.3 Milestone probe order list

| | |
|---|---|
| First-party | `docs/reference/gpu-execution-providers.md:33-35` — `TensorRTRtx → Migraphx → OpenVinoCatalog → Qnn → VitisAi → TensorRt → Cuda → OpenVino → DirectMl → Dnnl → Cpu` |
| Vendor | N/A (Trackdub planning order). |
| Classification | **vendor-correct (first-party drift, vs source)** — `src/Trackdub.Inference/Runtime/Planning/StageRuntimeRequirements.cs:20-32` (`Milestone5PlanningPolicy.SupportedProvidersThisMilestone`) includes **`ExecutionProviderKind.CoreMl`** between `OpenVino` and `DirectMl`. Both first-party docs (`gpu-execution-providers.md:33-34` and `windows-ml-stage-provider-matrix.md:13`) omit `CoreMl`. This is not vendor drift but source-vs-doc drift discovered during this reconciliation; recorded here because the prompt asks for exact replacement text wherever the first-party doc is wrong. Note `CoreMl` has no vendor counterpart in this corpus (CoreML EP is Apple/macOS; `cuda-ep`/`openvino-ep` vendor pages absent). |
| Retrieval | Query "Windows ML device policy does not select TensorRT RTX MaxPerformance catalog routes DirectML MIGraphX": top hits (phase-3 doc, ADR-0002, stage matrix) all restate the 11-item order without CoreMl — retrieval would *reinforce* the drift. Flagged. |
| Proposed edit | ```diff
- More precisely, `Milestone5PlanningPolicy.SupportedProvidersThisMilestone` orders
- all providers (`TensorRTRtx → Migraphx → OpenVinoCatalog → Qnn → VitisAi →
- TensorRt → Cuda → OpenVino → DirectMl → Dnnl → Cpu`), and each stage intersects
+ More precisely, `Milestone5PlanningPolicy.SupportedProvidersThisMilestone` orders
+ all providers (`TensorRTRtx → Migraphx → OpenVinoCatalog → Qnn → VitisAi →
+ TensorRt → Cuda → OpenVino → CoreMl → DirectMl → Dnnl → Cpu`), and each stage intersects
  that with its own allow-list.
``` |

### 2.4 Engine-family exclusion table

| | |
|---|---|
| First-party | `docs/reference/gpu-execution-providers.md:52-66` — ASR `whisper-onnx` (contrib ops), `whisper-genai` (GenAI crash), Translation `opus-mt`/`madlad` (ctor stack overflow), `phi-genai`, TextRefinement `qwen-instruct`, TTS `kokoro` (CPU-only, ConvTranspose), `chatterbox`/`cosyvoice`/`qwen3-tts`, LipSynthesis `latentsync-diffusion` (MultiHeadAttention) |
| Vendor | Vendor docs advertise broad ONNX support and do not enumerate per-model import failures (expected: crash/op-support specifics are workload-dependent). `ms-winml-supported-eps` only notes MIGraphX "not supported for GenAI scenarios today". |
| Classification | **agree / trackdub-specific** — every row was verified against `StageRuntimeRequirements.cs` (lines 79-81, 98-100, 148-155, 175-179, 188-190): all match, including `kokoro -> [ExecutionProviderKind.Cpu]` and the LatentSync MultiHeadAttention comment. `whisper-onnx` exclusion reason (contrib ops / stock-Olive graphs omit trt-rtx in supported_providers) matches source comment lines 76-78. |

### 2.5 Fatal-crash guards

| | |
|---|---|
| First-party | `docs/reference/gpu-execution-providers.md:67-72` — `ThrowIfGenAiTensorRtProvider` and `ThrowIfFatalTensorRtFamily` refuse before native call; smoke sweep bypasses allow-lists by design |
| Vendor | Silent (same as 1.7). **trackdub-deliberate**, same ADR-0002 Phase 6 canon; no additional edit beyond what 1.7 proposes. |

---

## Pair 3 — `docs/reference/windows-ml-*.md` <-> Microsoft Windows ML docs

First-party keys: `first-party/trackdub/docs/reference/windows-ml-stage-provider-matrix.md`, `windows-ml-phase-3-device-policies.md`, `windows-ml-phase-4-closeout.md`, `windows-ml-phase-5-catalog-eps.md` <-> `vendor/microsoft/winml-*` (served as `ms-winml-overview`, `ms-winml-supported-eps`, `ms-winml-select-eps`, `ms-winml-initialize-eps`, `ms-directml`).

### 3.1 Catalog EP set and EpNames

| | |
|---|---|
| First-party | `docs/reference/windows-ml-phase-5-catalog-eps.md:1,7-9` — Trackdub catalog scope: MIGraphX, OpenVINO, QNN, VitisAI (+ standalone OpenVINO dual path); TRT RTX explicitly not a catalog route |
| Vendor | `ms-winml-supported-eps`: included EPs = CPU + DirectML (legacy); downloadable catalog EPs (WinML 2.x) = MIGraphX (`MIGraphXExecutionProvider`), NvTensorRtRtx (`NvTensorRtRtxExecutionProvider`), OpenVINO (`OpenVINOExecutionProvider`), QNN (`QNNExecutionProvider`), VitisAI (`VitisAIExecutionProvider`), **WebGPU (Experimental, `WebGpuExecutionProvider`)**. |
| Classification | **agree** on the four Trackdub catalog families and their EpNames. Two deliberate scope differences, both fine: (a) Trackdub excludes `NvTensorRtRtx` from its catalog scope (trackdub-deliberate, ADR-0002 point 2 — standalone plugin instead); (b) Trackdub does not mention the experimental WebGPU EP (unverifiable scope gap: vendor offers it, first-party docs are silent; experimental + requires preview NuGet 2.4.66+ so it is reasonably out of Trackdub's stable scope — no edit, note only). Hardware requirements quoted nowhere in first-party docs, so MIGraphX "RDNA 3 or later, driver 25.10.13.09+" etc. have nothing to contradict. |
| Retrieval | Query "TensorRT RTX Windows ML catalog EP NvTensorRtRtx standalone plugin route" surfaces `ms-winml-supported-eps` chunk 1 (catalog EpName table incl. `NvTensorRtRtxExecutionProvider`) next to Trackdub chunks stating the opposite route for TRT RTX. **Contradiction-in-results flag (expected, not a defect)**: an agent could read the WinML table as proof TRT RTX is a catalog EP. The top-ranked Trackdub chunks (plugin doc, phase-5 doc, ADR-0002) do explain the split; rank order leads to the reconciled answer. Flagged as a retrieval-hygiene item, not a doc defect. |

### 3.2 Windows 11 24H2 requirement

| | |
|---|---|
| First-party | No first-party doc states the WinML catalog-EP OS floor. (Windows TFM `net10.0-windows10.0.19041.0` targets build 19041, a *different* thing: a compile target, not a runtime OS claim.) |
| Vendor | `ms-winml-overview`: "Hardware-optimized execution providers for NPUs and specific GPU hardware require Windows 11 version 24H2 (build 26100) or greater." `ms-winml-supported-eps`: catalog EPs "available on Windows 11 PCs running version 24H2 (build 26100) or greater". |
| Classification | **unverifiable -> gap**: first-party docs make no OS-requirement claim, so nothing to correct, but Trackdub's Windows ML docs never surface the 24H2 floor its own catalog routes implicitly depend on. Propose one sentence in the phase-5 catalog doc. |
| Proposed edit | ```diff
  # docs/reference/windows-ml-phase-5-catalog-eps.md
  Internal checklist for Intel OpenVINO, Qualcomm QNN, AMD MIGraphX, and AMD VitisAI **Windows ML catalog** routes. TensorRT RTX is not a Windows ML catalog route anymore; it uses the standalone ORT EP ABI plugin documented in [tensorrt-rtx-ep-abi-plugin.md](tensorrt-rtx-ep-abi-plugin.md).
+
+ Microsoft makes catalog EPs available only on Windows 11 24H2 (build 26100)+, per the Windows ML execution-providers page; on older Windows, discovery must report these EPs unavailable rather than partially ready.
```

### 3.3 Device-policy enum mapping (phase 3)

| | |
|---|---|
| First-party | `docs/reference/windows-ml-phase-3-device-policies.md:16-22` — `Explicit` -> none; `MaxPerformance` -> `MAX_PERFORMANCE`; `PreferNpu` -> `PREFER_NPU`; `MaxEfficiency` -> `MAX_EFFICIENCY`; `MinOverallPower` -> `MIN_OVERALL_POWER`; `DefaultRender` -> `DEFAULT_RENDER` (probe-gated); `MinPower` -> `MIN_POWER` (probe-gated); mutual exclusion rule 1; catalog-only rule 2 |
| Vendor | `ms-winml-select-eps`: explicit selection via `GetEpDevices` + `AppendExecutionProvider` (C#) / `AppendExecutionProvider_V2` (C++) is recommended first; device policies via `SessionOptions.SetEpSelectionPolicy` with `OrtExecutionProviderDevicePolicy` values "like `MAX_PERFORMANCE`, `PREFER_NPU`, `MAX_EFFICIENCY`, and more" (full enum deferred to ORT API docs, not in corpus). The mutual-exclusion rule (policy XOR explicit append) is Trackdub's own session-factory discipline; vendor shows them as two alternative mechanisms without stating an XOR. |
| Classification | **agree** on the four core policy names. The probe-gated `DEFAULT_RENDER` / `MIN_POWER` members cannot be confirmed from the fetched vendor set (ORT API enum doc not in corpus) -> **unverifiable** for those two rows specifically; keep as-is since the doc already describes them as capability-probed and behaviorally falling back to `Explicit`. Mutual exclusion and catalog-only scoping: **trackdub-deliberate** (session factory discipline, ADR-0002 Phase 3 "Non-default values call `SessionOptions.SetEpSelectionPolicy` ... and skip explicit catalog-device append"). |
| Retrieval | Query "device policy MAX_PERFORMANCE SetEpSelectionPolicy mutual exclusion explicit append": top hit is the phase-3 doc itself (rules list), then `ort-plugin-ep-usage` chunk 3 showing the two APIs as separate recipes. Leads to the answer; the ORT chunk shows policies as an alternative, not contradicting the XOR. No contradiction. |

### 3.4 "Policy does not select TensorRT RTX"

| | |
|---|---|
| First-party | `docs/reference/windows-ml-phase-3-device-policies.md:32` (rule 2) and stage-matrix line 88 |
| Vendor | `nv-trt-rtx-arch-overview`: "TensorRT-RTX is the default EP when Windows ML runs on supported RTX hardware" — i.e. under *Microsoft's* Windows ML integration, a policy-driven session on an RTX box may well land on TRT-RTX. `ms-winml-select-eps` device-policy example (MAX_EFFICIENCY, "typically will choose an NPU if available") does not name TRT-RTX either way. |
| Classification | **trackdub-deliberate** — Trackdub's policy surface is scoped to its own catalog routes and deliberately never selects TRT RTX via policy because Trackdub's TRT RTX is not registered through WinML at all (it is a separately-registered plugin; a WinML policy cannot see it in Trackdub's process). Consistent with ADR-0002 point 2 and the mutual-exclusion rules. The vendor's "default EP on RTX" sentence describes the Microsoft-managed WinML route, which Trackdub does not use for TRT RTX. Add the one-sentence cross-reference the prompt's classification scheme asks for, so the vendor quote can't be read as contradicting Trackdub behavior. |
| Retrieval | Query D7 results: phase-3 doc rule 2 is the top hit; ADR-0002 chunks follow; `nv-trt-rtx-arch-overview` "default EP" text was retrievable via other queries. Leads to reconciled answer. |
| Proposed edit | ```diff
  2. **Catalog only:** Policy applies to Windows ML catalog routes (`DirectMl`, `MIGraphX`). It does **not** select TensorRT RTX — TRT RTX uses the standalone EP ABI plugin (`trackdub providers trt-rtx`, Model Manager Install, or `Fetch-TrtRtxEp.ps1`).
+    (NVIDIA's docs describe TRT-RTX as the default EP when *Microsoft's* Windows ML manages the session; in Trackdub the plugin is registered outside the WinML catalog, so a WinML device policy never sees it.)
```

### 3.5 Phase-5 stub honesty

| | |
|---|---|
| First-party | `docs/reference/windows-ml-phase-5-catalog-eps.md:24-28` — stub kinds return **unavailable** in discovery, `NotSupportedException` on session append, `#TODO(phase-5-catalog-ep)` marker; ADR-0002 5c non-goals: "Stubs must report unavailable ... never 'GPU ready'". |
| Vendor | `ms-winml-initialize-eps` `ReadyState` model: NotPresent / NotReady / Ready + `TryRegister()`; `EnsureAndRegisterCertifiedAsync()` downloads + registers all. |
| Classification | **agree (trackdub-deliberate shape)** — Trackdub's unavailable-until-gated discipline is stricter than the vendor's catalog-ReadyState model and matches AGENTS.md "Never fake readiness". No mismatch. |

### 3.6 Stage matrix dated evidence rows

| | |
|---|---|
| First-party | `docs/reference/windows-ml-stage-provider-matrix.md:46-58, 73-79, 93-109` — dated manual smoke rows (RTX 5080 2026-05-23, RTX 5070 plugin 0.3.0 cu12), including "pending" rows |
| Vendor | N/A (Trackdub test evidence). **agree / out of vendor scope.** Rows honestly mark pending vs pass; no fake readiness. Note the two smoke tables name different host GPUs (5080 benchmark host, 5070 plugin smoke) — internally consistent because they are different dated runs. |

### 3.7 Expected_runtime canonical token

| | |
|---|---|
| First-party | `docs/reference/windows-ml-stage-provider-matrix.md:64-70` — canonical token `windows-ml|onnxruntime-migraphx|onnxruntime-directml`, legacy token still parseable, planner does not read the field |
| Vendor | N/A. **agree / Trackdub manifest governance.** Consistent with ADR-0002 consequences ("expected_runtime tokens use `trt-rtx|windows-ml|onnxruntime-migraphx|onnxruntime-directml`"). |

### 3.8 ADR-0002 broken internal links (`docs/internal/`)

| | |
|---|---|
| First-party | `docs/decisions/ADR-0002-windows-ml-provider-strategy.md:64,90,100,111,115` and `docs/decisions/decisions.md` (same pattern) link to `../internal/windows-ml-phase-3-device-policies.md` etc. |
| Vendor | N/A. **needs-human (mechanical)**: `docs/internal/` does not exist in this repo (Glob: 0 files); the targets live in `docs/reference/` and the ADR-0002 *copy in the corpus chunks* shows the same broken relative links, so RAG retrieval returns dead-link context for those sections. The `reference/*.md` files themselves link correctly (`../adr/ADR-0002...`). Doc-drift, low-risk, exact fix obvious (replace `../internal/` with `../reference/`), but the file is an ADR and edits are out of scope for this read-only report. Recorded as needs-human with proposed diff for a follow-up. |
| Proposed edit | ```diff
  # docs/decisions/ADR-0002-windows-ml-provider-strategy.md (5 occurrences)
- Details: [windows-ml-phase-3-device-policies.md](../internal/windows-ml-phase-3-device-policies.md).
+ Details: [windows-ml-phase-3-device-policies.md](../reference/windows-ml-phase-3-device-policies.md).
- ... [windows-ml-stage-provider-matrix.md](../internal/windows-ml-stage-provider-matrix.md)
+ ... [windows-ml-stage-provider-matrix.md](../reference/windows-ml-stage-provider-matrix.md)
- ... [windows-ml-phase-5-catalog-eps.md](../internal/windows-ml-phase-5-catalog-eps.md)
+ ... [windows-ml-phase-5-catalog-eps.md](../reference/windows-ml-phase-5-catalog-eps.md)
- ... [windows-ml-phase-4-closeout.md](../internal/windows-ml-phase-4-closeout.md)
+ ... [windows-ml-phase-4-closeout.md](../reference/windows-ml-phase-4-closeout.md)
- - Internal seams: [`docs/internal/migraphx-phase0-seams.md`](../internal/migraphx-phase0-seams.md)
+ - Seams: [`docs/reference/migraphx-phase0-seams.md`](../reference/migraphx-phase0-seams.md)
``` |

---

## Pair 4 — `docs/reference/migraphx-phase0-seams.md` <-> AMD MIGraphX docs — **UNBLOCKED (follow-up pass)**

First-party key: `first-party/trackdub/docs/reference/migraphx-phase0-seams.md`. Originally blocked when the vendor sources looked unreachable; the follow-up pass fetched all six mapped sources from hosted R2 (`vendor/amd/migraphx.md`, `migraphx-install.md`, `migraphx-driver.md`, `migraphx-cpp.md`, `migraphx-operators.md`, `migraphx-quantization.md` — all `found: true`, not truncated).

| Claim (doc is a pure internal seam table) | Vendor check | Verdict |
|---|---|---|
| Every row is a Trackdub code pointer: `ExecutionProviderKind` in `RuntimePlanning.cs`, `Milestone5PlanningPolicy` / `StageRuntimeRequirementsCatalog` in `StageRuntimeRequirements.cs`, `OnnxExecutionProviderDiscovery`, `WindowsMlExecutionProviderBootstrapper.Windows.cs`, `OnnxExecutionSessionFactory.CreateSessionOptions`, `OnnxExecutionProviderSmokeTester`, `WindowsDeviceEnumerator` / `LinuxDeviceEnumerator`, `HardwareOverrideCatalog`, `CompositionRoot.AddInference`, ADR-0002 link | The first-party doc asserts **no upstream facts**: no MIGraphX version, API name, signature, default, flag, or hardware requirement appears anywhere in it. Vendor docs (MIGraphX 2.15.0 / ROCm 7.2.4, "Applies to Linux" landing; install, driver, C++ API, operators, quantization pages) describe the AMD library and contain nothing for these pointers to contradict. One near-miss checked: the doc's "WinML catalog" row refers to Trackdub's Windows ML bootstrap files, not any AMD doc content. | **agree — "no upstream claims" conclusion confirmed** against the now-fetchable vendor canon. The pair carries zero vendor-drift risk by construction. No edit. |

## Pair 5 — `docs/reference/olive-recipe-pilot.md` <-> `vendor/olive/*` — **UNBLOCKED (follow-up pass)**

All mapped Olive sources fetched from hosted R2 (`olive-landing`, `olive-why`, `olive-getting-started`, `olive-installation`, `olive-python-api`, `olive-cli-optimize`, `olive-cli-quantize`, `olive-build-workflow`, `olive-pass-configuration`, `olive-features-index`, `olive-quantization`, `olive-onnx-transformations`, `olive-huggingface-integration`, `olive-reference` — all `found: true`, not truncated; corpus pins Olive 0.13.0). Per AGENTS.md conflict order, Trackdub-behavior claims were also verified against source.

| # | Claim (first-party quote) | Vendor quote (R2 key) | Source check | Classification |
|---|---|---|---|---|
| 5.1 | Pilot scope: "manifest `recipe_bindings` on pilot families `whisper-genai`, `whisper-onnx`, and `phi-genai`" (line 7); binding fields `provider`, `precision`, `config_relative_path` (lines 27-33) | Vendor docs are silent on `recipe_bindings` — the concept is Trackdub's manifest extension riding on Olive config files. Nearest vendor text: `olive-cli-optimize.md` "olive optimize primarily requests desired model precision and intended ExecutionProvider ... will generate model optimization recipe as per user request"; `olive-build-workflow.md` "olive run --config my_model_processing_description.json". | `bundled-models.manifest.json` has `recipe_bindings` arrays (lines 112, 244); `ModelManifestLoader.cs:728-743` parses/validates them; `model-manifest.schema.json:260` declares the field. | **agree** — Trackdub-specific manifest mechanism, vendor-silent by design; nothing to correct. |
| 5.2 | `TRACKDUB_OLIVE_RECIPES_ROOT` env var; manifest paths relative to that root; unset/missing config falls back to `auto-opt` and logs the reason (lines 18-23) | No fetched vendor doc contains `OLIVE_RECIPES`, `config_relative`, or any recipe-root concept. `auto-opt` matches vendor vocabulary: `olive-why.md` lists `olive auto-opt` among "Easy-to-use CLI for common model optimization tasks". | Trackdub env var + fallback; vendor-silent wiring. | **agree** — Trackdub-specific; nothing contradicted. |
| 5.3 | "Developer override (ModelLab only): `--olive-recipe-config <path>` runs `olive run --run-config <path>`" (line 9, example line 39) | **`olive-build-workflow.md`: "one simple command will process the model ... `olive run --config my_model_processing_description.json`"** — the pinned Olive 0.13.0 CLI flag is `--config`. Zero hits for `--run-config` across all fetched Olive docs. | `ModelLabCommand.cs:438` passes `"--run-config"` to the olive CLI — the doc accurately describes the code, but code and doc both differ from the pinned vendor flag. `OliveModelOptimizationService.cs:375` records CommandKind `"run --config"` — Trackdub's own provenance string uses the vendor spelling, exposing the inconsistency inside one repo. | **vendor-correct** — first-party doc (and the CLI argument it documents) drifts from the pinned vendor doc. Either Trackdub runs a non-0.13.0 Olive whose flag is `--run-config`, or the flag is wrong; the code-side flag needs a human decision, so the proposed edit hedges rather than asserting the vendor flag is right. |
| 5.4 | "Olive stdout lines matching `Step N/M` are also emitted as `[progress] Step N/M`" (line 47) | No fetched vendor doc documents an Olive stdout `Step N/M` line format (zero hits for step/progress output patterns across all 13 sources). | `OliveOptimizationProgress.cs` (Contracts): regex `Step\s+(\d+)\s*/\s*(\d+)` (IgnoreCase), emits `[progress] Step {n}/{m}` — matches the doc exactly. | **agree** — Trackdub parsing behavior of (corpus-undocumented) Olive stdout; verified against source. |
| 5.5 | "optimization uses **shared bundled folder** `auto-opt` (`UseSharedComponentCache`) instead of per-component loops" (line 51) | Vendor 0.13.0 docs mention only "Share models cache on Azure / Shared cache" (`olive-features-index.md`); no `UseSharedComponentCache` flag anywhere. | `ModelOptimizationRequest.cs:16`: `bool UseSharedComponentCache = false` — the Trackdub flag exists in source with that exact name. | **agree (Trackdub-specific)** — vendor-silent mechanism name, verified in source. |

Retrieval check (hosted index): query "olive run --run-config recipe config ModelLab override" — top hit `first-party/trackdub/docs/reference/olive-recipe-pilot.md` (0.99), remaining hits are bundled olive-recipe READMEs. Retrieval surfaces only the first-party (drifted) form; no vendor chunk for `olive run --config` ranked, so retrieval cannot catch 5.3 — expected, since the flag difference is one token inside a large doc.

Proposed edit (5.3):

```diff
- - **Developer override (ModelLab only):** `--olive-recipe-config <path>` runs `olive run --run-config <path>`.
+ - **Developer override (ModelLab only):** `--olive-recipe-config <path>` runs the recipe config through `olive run`. The pinned vendor doc (Olive 0.13.0) uses `olive run --config <path>`; Trackdub's ModelLab currently passes `--run-config` (`ModelLabCommand.cs`) — verify against the installed Olive version before relying on the flag name.
```

## Pair 6 — `runtime/trt-rtx-ep.manifest.json` + `AGENTS.md` <-> vendor pin

- Manifest version `0.3.0` / CUDA `cu12` / release asset URLs / SHA-256 / sizes: **agree** with `gh-ep-abi-v030` release tag (v0.3.0, cu12 + cu12-linux-x64 assets present, 6 assets) and with the corpus pin block (`matchesCorpusPin: true`). No mismatch.
- `AGENTS.md:8` "Never fake readiness: Provider registered != model downloaded != stage ran != stage succeeded": restated faithfully by tensorrt-rtx-ep-abi-plugin.md readiness list; no vendor doc contradicts the principle. **agree.**
- ADR-0002 as canon: all trackdub-deliberate classifications above cite it; its statements match the reference docs' behavior descriptions. One defect found in ADR-0002 itself: broken `docs/internal/` links (3.8).

---

## Counts (after follow-up pass)

| Classification | Count |
|---|---|
| agree (no delta) | 18 (previous 13 + Pair 4's confirmed no-upstream-claims row + Pair 5's 5.1, 5.2, 5.4, 5.5) |
| vendor-correct (doc drift, exact replacement proposed) | 2 (2.3 probe order omits `CoreMl`; 5.3 `olive run --run-config` vs pinned vendor 0.13.0 `olive run --config`) |
| trackdub-deliberate (keep + add vendor-default sentence; 4 edits proposed: 1.2, 1.6, 1.7, 3.4) | 6 (1.2, 1.6, 1.7, 1.12, 2.5, 3.4) |
| unverifiable (vendor silent in reachable corpus, or first-party gap with no claim to contradict) | 5 (1.5 ORT floor gap, 1.8 EP-options gap, 1.9 ORT_LOG_SEVERITY_LEVEL, 3.2 OS-floor gap, 3.3 DEFAULT_RENDER/MIN_POWER rows) |
| needs-human | 3 (2.2 CLI aliasing decision, 3.8 ADR-0002 broken links, plus 5.3's code-side flag: `ModelLabCommand.cs:438` passes `--run-config` to the olive CLI — the doc fix is proposed, but whether the code's flag is correct against the installed Olive needs a human) |
| **Total claims checked** | **34** (33 claim rows across Pairs 1-6; claim 5.3 spans two classifications — doc text = vendor-correct, code-side flag = needs-human) |
| Blocked pairs | **0** — Pairs 4 and 5 unblocked in the follow-up pass via hosted R2 fetches (`.md`-suffixed vendor keys). All 40 mapped vendor keys probed `found: true`; no fetch truncated at 200000 chars. |

Every fetched vendor doc returned `truncated: false` at 200000 chars; there are no remaining blocks.

## Top 5 highest-risk deltas (after follow-up pass)

1. **Probe-order list omits `CoreMl`** (2.3 / both `gpu-execution-providers.md` and `windows-ml-stage-provider-matrix.md`) — the only claim where first-party docs are simply wrong against source; every agent reading either doc will mis-state the planner order, and the hosted-index retrieval check *reinforces* the drift (all 4 hits repeat the wrong 10-item list). **Fixed in-repo 2026-09-22 (both docs now list `CoreMl` between `OpenVino` and `DirectMl`); corpus re-sync still required for the index to heal.**
2. **GenAI+TRT-RTX exclusion vs vendor advertising** (1.7) — NVIDIA's current docs (confirmed under the mapped `vendor/nvidia/trt-rtx-arch.md`) advertise ORT GenAI on TRT-RTX while Trackdub (correctly, per ADR-0002 Phase 6) hard-excludes it after an observed host-terminating crash; an agent weighing vendor chunks equally could re-enable a route that kills the process. Hosted-index retrieval currently ranks only the correct first-party answer, but the vendor contradiction is one query away.
3. **`olive run --run-config` vs pinned vendor `--config`** (5.3, new in follow-up) — first-party doc and `ModelLabCommand.cs:438` both pass `--run-config`, but the pinned Olive 0.13.0 doc uses `olive run --config`; Trackdub's own provenance string (`"run --config"`) already uses the vendor spelling, so the repo contradicts itself about which CLI it invokes. Retrieval cannot catch this (no vendor chunk ranks). Code-side flag needs a human.
4. **Missing ORT floor for the pinned EP** (1.5) — first-party docs never state EP ABI 0.3.0 needs ORT 1.25.0+ (negotiating 1.24-1.26+); the hosted-index retrieval check found the question *unanswerable from the index* (the vendor compatibility-matrix chunk didn't rank in the top 8; suspected stale/under-ranked item), so an agent would have to fall back to reading the vendor README directly. The proposed one-sentence doc edit fixes both doc and retrieval.
5. **`NvTensorRtRtxExecutionProvider` vs `NvTensorRTRTXExecutionProvider` confusion risk** (1.2) — two live devices with near-identical spellings on different routes (WinML catalog vs standalone plugin); the doc's "deprecated wiring" framing mislabels a still-supported Microsoft catalog EP. Hosted-index retrieval leads to the right answer (helped by the synthesized `docs-rag-pin.md` at rank 1), but no vendor chunk ranks to confirm the catalog EP's separate existence — and that rank-1 doc is repo-invisible (see provenance hygiene note), so the correctness currently rests on a synthesized artifact.

Demoted from the first-pass list: **ADR-0002 broken `docs/internal/` links** (3.8) — still a real, mechanically-fixable defect (5 dead links, degrades RAG context for those sections), but the follow-up pass surfaced higher-severity items (5.3 self-contradiction; the now-measured retrieval failures for 2.3/1.5). It remains needs-human with a proposed diff.

## R2-index retrieval checks (hosted `/v1/search`, follow-up pass)

One query per top delta, scope `all`, limit 8, against the hosted AI Search index (197 objects, ~25 `outdated` capacity-error items — stale-chunk results flagged). Findings:

**1. CoreMl probe-order drift** — query "Milestone5PlanningPolicy SupportedProvidersThisMilestone provider probe order CoreMl": 4 hits, all first-party (`windows-ml-stage-provider-matrix.md` 0.98, `migraphx-phase0-seams.md` 0.81, `gpu-execution-providers.md` 0.98, ADR-0002 0.95). **Verdict: retrieval fails to lead an agent to the reconciled answer.** Every hit repeats the 10-item order *without* `CoreMl`, and the highest-matching chunks are the drifted docs themselves — retrieval reinforces the drift. Note `CoreMl` matched nothing distinct (only docs that mention it via the enum list in source-adjacent docs); the planner-order chunks are all pre-drift text. No vendor chunk participated (planner order is Trackdub-internal). Not a stale-chunk artifact: the on-disk docs match their indexed chunks; the index is faithfully serving the wrong text. Fixing the docs then re-syncing the corpus is the only path.

**2. GenAI-on-TRT-RTX exclusion** — query "ORT GenAI NvTensorRtRtx terminates process qwen-instruct TensorRT exclusion": top hits are `gpu-execution-providers.md` (0.97), `tensorrt-rtx-ep-abi-plugin.md` (1.00), ADR-0002 (0.94) — all carry the reconciled (Trackdub) answer with the crash rationale. Vendor participation: only `vendor/nvidia/ep-abi-readme.md` (0.53, generic EP description, no GenAI claim). **Verdict: leads to the reconciled answer; no contradicting vendor chunk ranked.** The GenAI-advertising `trt-rtx-arch.md` vendor chunks did not surface for this query (they did in the substitute-corpus pass), so the hosted index currently gives the correct picture without contradiction.

**3. NvTensorRtRtx catalog-vs-plugin spelling** — query "NvTensorRtRtxExecutionProvider NvTensorRTRTXExecutionProvider Windows ML catalog standalone plugin": 8 hits, all first-party: `docs-rag-pin.md` (1.0, rank 1), `tensorrt-rtx-ep-abi-plugin.md` (1.0), `windows-ml-phase-5-catalog-eps.md` (1.0), `trackdub-gated/AGENTS.md` (1.0), ADR-0002 twice (1.0/0.99), `cloud-operations.md` (0.99), `windows-ml-stage-provider-matrix.md` (0.98). **Verdict: leads to the reconciled answer.** The synthesized `docs-rag-pin.md` ranks #1 with exactly the right guidance ("Do not treat Windows ML catalog NvTensorRtRtxExecutionProvider as the primary route") — the corpus pin note is doing its job here. **No vendor chunk (e.g. `ms-winml-eps` listing the catalog EpName) ranked**, so no spelling contradiction appears in these results — softer than the substitute-corpus pass, where the WinML EpName table surfaced alongside. Caveat: with all-top-8 first-party, an agent gets no vendor-side confirmation that the `NvTensorRtRtx` spelling belongs to a real, separate Windows ML catalog EP; the first-party framing ("deprecated wiring", per finding 1.2 slightly mislabeled) is the only framing on offer.

**4. ORT 1.25.0+ floor** — query "EP ABI 0.3.0 ONNX Runtime 1.25 compatibility matrix TensorRT RTX 1.5": 8 hits, all first-party again: `docs-rag-pin.md` (0.93, rank 1), ADR-0002 (0.89 x2), `windows-ml-stage-provider-matrix.md` (0.89), `tensorrt-rtx-ep-abi-plugin.md` (0.83), gated AGENTS.md (0.87), `gpu-execution-providers.md` (0.81), `windows-ml-stage-provider-matrix.md` (0.81). **Verdict: retrieval does NOT reach the reconciled answer.** The gap identified in 1.5 is an *absence*: no first-party doc states the ORT floor, and the vendor chunk that answers the query (`vendor/nvidia/ep-abi-readme.md` compatibility matrix: EP 0.3 -> ORT 1.25.0+ / TRT-RTX 1.5.x.x) did not rank in the top 8 — plausible stale-index behavior (the README's compatibility-matrix chunk may be among the ~25 outdated items) or simply weak term overlap ("ONNX Runtime 1.25" appears in the vendor doc as "1.25.0+"). An agent asking this question gets pin assertions but not the version floor. Flagged as **suspected stale/under-ranked vendor chunk**; the gap edit proposed in 1.5 would fix this regardless of index state.

**5. Engine-cache clearing** — query "engine cache clear stale compiled engines driver change GPU swap": **0 hits** (`matchMode: union`, empty result). Re-run variants: "runtime cache EngineCache" (5 hits, top: `ARCHITECTURE-source.md` 0.93 first-party, then vendor `trt-rtx-quick-start.md` 0.90 / `trt-rtx-first-engine.md` 0.85 / `trt-rtx-how.md` 0.65); "trackdub cache clear engines" (8 hits, top: `ARCHITECTURE-source.md` 0.94). **Verdict: partially leads to the answer, with a staleness signal.** The exact query that names the delta returns nothing — surprising because `tensorrt-rtx-ep-abi-plugin.md` contains "Clear the engine cache after a GPU driver change, GPU swap, or TRT RTX EP version bump" and "trackdub cache clear engines"; its R2 bytes (fetched, `size` 13139) contain both strings, and other queries ("RegisterExecutionProviderLibrary NvTensorRTRTXExecutionProvider", "TRT_RTX_EP_DIR plugin directory locator") rank that doc #1. So the plugin doc is indexed, but its cache-section chunk missed these terms — either chunk boundaries split the cache section from its trigger phrases, or that chunk is among the ~25 outdated items. **Flagged as suspected stale/undersized chunk.** `ARCHITECTURE-source.md` (a real committed doc, identical to its staged copy) covers the guidance ("If persistent inference crashes occur ... or if the hardware is changed, users should manually delete all files in `%LOCALAPPDATA%\Trackdub\EngineCache`"), so an agent still lands on compatible guidance — from architecture docs rather than the plugin reference. Vendor quick-start/first-engine chunks that ranked describe `--runtimeCacheFile` (positive use), not invalidation.

**Summary:** 2 of 5 queries (GenAI exclusion, EP-name spelling) lead cleanly to the reconciled answer; 2 (probe order, ORT floor) fail because the needed truth is absent from the top-ranked first-party docs (drift text / missing fact) and the vendor answer didn't rank; 1 (engine cache) hit a likely stale/undersized chunk returning 0 results for its exact phrasing. Two suspected stale-index artifacts flagged (queries 4 and 5); the probe-order reinforcement (query 1) is not staleness — the index faithfully serves drifted docs. Two cross-cutting hygiene findings: (a) the synthesized `docs-rag-pin.md` (repo-invisible, see provenance) is the top-ranked hit on two queries and the sole correct answer source on one of them; (b) on all five queries the top ranks are first-party-heavy, so any doc drift propagates into answers almost unopposed — doc fixes must be followed by a corpus re-sync for the index to heal.
