{
  "id": "FEAT-003",
  "type": "test",
  "description": "Add xUnit tests in tests/Trackdub.Sdk.Tests that lock in the new wizard-state encapsulation and prove the state-object-to-TuiPipelineRunOptions mapping is correct, so future option additions cannot silently drift. Tests must exercise the new code path (the mapping method and/or the wizard flow via TestConsole) and would fail if the mapping were reverted or mis-wired. Cover the option-enabled path, the defaults/disabled path, and a subtitle/format-sentinel path.",
  "status": "completed",
  "steps": [
    "Add tests to tests/Trackdub.Sdk.Tests (either extend TrackdubTuiTests.cs or add a focused new file such as PipelineWizardStateTests.cs in namespace Trackdub.Sdk.Tests). Follow existing patterns: xUnit [Fact], Spectre.Console.Testing TestConsole where a full wizard walk is exercised, and the fake-backed factory pattern only if a session is needed. The new state type and mapping are internal but visible via InternalsVisibleTo Trackdub.Sdk.Tests.",
    "Test 1 (mapping — all options on): construct the new PipelineWizardState with non-default values (VoiceClone=true, TimbrePolish=false, RestorePan=true, MatchLoudness=true, AsrRefinement=true, BurnIn=true, ForceRerun=true, ExportFormat=\"mkv\", SubtitleSource=\"bilingual\", SubtitleFormats=[\"vtt\"], VideoEncoder=\"nvenc\", TargetLanguageOverride=\"es\"), call the mapping (ToRunOptions()), and assert every corresponding TuiPipelineRunOptions property matches (UseVoiceCloning, ApplyTimbrePolish, RestoreOriginalPan, MatchOriginalLoudness, EnableAsrTextRefinement, BurnInSubtitles, ForceRerun, ExportFormat, SubtitleSource, SubtitleFormats, VideoEncoderKey, TargetLanguageOverride). This fails if any mapping line is dropped or crossed.",
    "Test 2 (defaults): construct a fresh PipelineWizardState and assert its mapped TuiPipelineRunOptions equals the wizard's default expectations — specifically ApplyTimbrePolish is true by default and every other configurable option is at its default (false / null). This locks the TimbrePolish=true default that matches TuiPipelineRunOptions.ApplyTimbrePolish.",
    "Test 3 (reset single-source-of-truth): mutate a state instance, then simulate reset the same way ResetWizardState does (fresh instance) and assert all fields returned to defaults — or, if practical, drive PipelineTuiScreen through 'g' -> Configure and back to confirm reset semantics. Keep it deterministic and offline.",
    "If feasible without excessive reflection, add one end-to-end-ish TestConsole walk of the advanced picker toggling one option (e.g. force rerun) and assert the label reflects state, reinforcing that handlers read/write the single object. Prefer clarity over exhaustive UI simulation; the mapping tests are the primary guard.",
    "Run the new tests and confirm they pass; then temporarily reason about whether reverting one mapping line would fail Test 1 (it must)."
  ],
  "acceptance_criteria": [
    "New tests exist in tests/Trackdub.Sdk.Tests and reference the new PipelineWizardState type and its mapping to TuiPipelineRunOptions.",
    "Tests cover: all-options-enabled mapping, default/disabled mapping (including ApplyTimbrePolish default true), and reset-to-defaults semantics.",
    "The mapping test asserts each TuiPipelineRunOptions property individually so a dropped or crossed mapping line makes it fail.",
    "'dotnet test tests/Trackdub.Sdk.Tests --no-restore -m:1' passes including the new tests.",
    "Tests are deterministic and offline (no models/FFmpeg/network required)."
  ],
  "verification": [
    "dotnet test tests/Trackdub.Sdk.Tests --no-restore -m:1",
    "dotnet format Trackdub.slnx --verify-no-changes"
  ],
  "blocked_reason": null,
  "findings": "COMPLETED. Added focused xUnit coverage locking in the encapsulated wizard state and its single mapping to PipelineHandler.TuiPipelineRunOptions.\n\nNEW FILE tests/Trackdub.Sdk.Tests/PipelineWizardStateTests.cs (namespace Trackdub.Sdk.Tests, public sealed class PipelineWizardStateTests). 4 new [Fact] tests:\n1. ToRunOptions_AllOptionsConfigured_MapsEveryPropertyIndividually — builds PipelineWizardState with all non-default values (VoiceClone=true, TimbrePolish=false, RestorePan=true, MatchLoudness=true, AsrRefinement=true, BurnIn=true, ForceRerun=true, ExportFormat=\"mkv\", SubtitleSource=\"bilingual\", SubtitleFormats=[\"vtt\"], VideoEncoder=\"nvenc\", TargetLanguageOverride=\"es\"), calls ToRunOptions(), and asserts EACH of the 12 TuiPipelineRunOptions properties individually (UseVoiceCloning, ApplyTimbrePolish, RestoreOriginalPan, MatchOriginalLoudness, EnableAsrTextRefinement, BurnInSubtitles, ForceRerun, ExportFormat, SubtitleSource, SubtitleFormats, VideoEncoderKey, TargetLanguageOverride). Reverting any single mapping line makes this fail (e.g. dropping RestoreOriginalPan=RestorePan leaves it false vs asserted true).\n2. ToRunOptions_FreshState_MatchesWizardDefaults — fresh PipelineWizardState().ToRunOptions(): asserts ApplyTimbrePolish is true and every other option is default (false / null incl. SubtitleFormats null). Locks the TimbrePolish=true default matching TuiPipelineRunOptions.ApplyTimbrePolish.\n3. FreshState_HasWizardDefaults — asserts the state object's own field defaults (TimbrePolish=true, rest false/null).\n4. ResetSemantics_FreshInstanceRestoresAllDefaults — mutates an instance, then asserts a fresh instance (the way ResetWizardState assigns `new PipelineWizardState()`) has all defaults restored; also confirms the mutated instance genuinely diverged first.\n\nTests are deterministic and fully offline (no models/FFmpeg/network); they reference internals via existing InternalsVisibleTo Trackdub.Sdk.Tests.\n\nVerification (SDK 10.0.401 via mise trust):\n- dotnet --version => 10.0.401.\n- dotnet build Trackdub.slnx --configuration Release -m:1 -warnaserror => Build succeeded, 0 Warning(s), 0 Error(s).\n- dotnet test tests/Trackdub.Sdk.Tests --configuration Release --no-build -m:1 => Passed! Failed: 0, Passed: 400, Skipped: 2, Total: 402 (up from 396 in FEAT-002 baseline; +4 new tests; 2 skips are the model-dependent HeadlessPipelineSmoke tests, unchanged).\n- dotnet format Trackdub.slnx --verify-no-changes => exit 0 (clean).\n\nCommitted locally (not pushed) as 'Add PipelineWizardState mapping and reset tests'. mise.toml remains modified/unstaged (the FEAT-001 SDK pin), same as FEAT-002."
}
