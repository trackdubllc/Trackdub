# Openai-clip-vit-base-patch16 Model Optimization

### QNN-GPU:

Please install Olive directly using:

```bash
pip install olive-ai
```

To run the config:

```bash
olive run --config config_gpu_fp32.json
```

✅ Optimized model saved in: `output/`
