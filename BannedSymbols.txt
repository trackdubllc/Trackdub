M:System.IO.Path.Combine(System.String,System.String);Use Path.Join instead - Path.Combine silently drops earlier segments if a later one is rooted, which CodeQL/CodeFactor flag on every call whose argument isn't provably non-rooted. Path.Join has no such reset behavior.
M:System.IO.Path.Combine(System.String,System.String,System.String);Use Path.Join instead - Path.Combine silently drops earlier segments if a later one is rooted, which CodeQL/CodeFactor flag on every call whose argument isn't provably non-rooted. Path.Join has no such reset behavior.
M:System.IO.Path.Combine(System.String,System.String,System.String,System.String);Use Path.Join instead - Path.Combine silently drops earlier segments if a later one is rooted, which CodeQL/CodeFactor flag on every call whose argument isn't provably non-rooted. Path.Join has no such reset behavior.
M:System.IO.Path.Combine(System.String[]);Use Path.Join instead - Path.Combine silently drops earlier segments if a later one is rooted, which CodeQL/CodeFactor flag on every call whose argument isn't provably non-rooted. Path.Join has no such reset behavior.
